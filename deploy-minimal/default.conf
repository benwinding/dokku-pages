server {
  listen       5000;
  server_name  localhost;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;

    # Try to enforce trailing slashes
    # rewrite ^([^.]*[^/])$ $1/ permanent;

    CORS_CONFIG
  }

  error_page   500 502 503 504  /50x.html;
  location = /50x.html {
    root   /usr/share/nginx/html;
  }
}